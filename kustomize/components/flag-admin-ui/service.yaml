apiVersion: v1
kind: Service
metadata:
  name: flag-admin-ui
  namespace: astroshop
  labels:
    app.kubernetes.io/name: flag-admin-ui
    app.kubernetes.io/component: admin
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: flag-admin-ui
  ports:
    - name: http
      port: 8080
      targetPort: 8080
